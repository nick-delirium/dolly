---
description: Show the dolly task board
argument-hint: "[--all] [--status working]"
allowed-tools: Bash(dolly:*)
---

!`dolly board $ARGUMENTS`

Report it compactly: what is in flight, what waits on human review (`validating`), what is stale. Name the active task and suggest one next action.
